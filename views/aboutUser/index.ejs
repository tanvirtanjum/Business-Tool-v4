<% if (type == 1) { %>
  <%-include('../adminDash/common.ejs') -%>
<% } %>

<% if (type == 2) { %>
  <%-include('../managerDash/common.ejs') -%>
<% } %>

<% if (type == 3) { %>
  <%-include('../salesmanDash/common.ejs') -%>
<% } %>

<% if (type == 4) { %>
  <%-include('../DeliverymanDash/common.ejs') -%>
<% } %>

<% if (type == 5) { %>
  <%-include('../customerDash/common.ejs') -%>
<% } %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" type="text/css" href="../assets/styles/common.css">
    <link rel="stylesheet" type="text/css" href="../assets/styles/about.css">
</head>
<body>
  <div class="box">
    <form method="POST">
        <h1>Profile Info <i class="fas fa-users"></i></h1>
        <p></p>Username<br>
        <input type="text" name="username" readonly value="<%= user[0].EmpID %><%= user[0].cusid %>"><br>
        <p>Full Name</p><br>
        <input type="text" name="fullname" readonly value="<%= user[0].E_NAME %><%= user[0].name %>"><br>
        <p>Designation</p><br>
        <input type="text" name="designation" readonly value="<%= user[0].DID %><%= user[0].design %>"><br>
        <p>Email<i class="far fa-envelope"></i></p><br>
        <input type="email" name="email" readonly value="<%= user[0].E_MAIL %><%= user[0].email %>"><br>
        <p>Mobile No.</p><br>
        <input type="number" name="mobile" readonly value="<%= user[0].E_MOB %><%= user[0].mobile %>"><br>
        <p>Join Date</p>
        <input type="text" name="joinDate" readonly value="<%= user[0].JOIN_DATE %><%= user[0].reg_date %>"><br>
    </form>
    <a href='/aboutUser/editProfile'> <input type="submit" value="Edit Profile"> </a>
  </div>
</body>
</html>
