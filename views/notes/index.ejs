<% if (type == 1) { %>
  <%-include('../adminDash/common.ejs') -%>
<% } %>

<% if (type == 2) { %>
  <%-include('../managerDash/common.ejs') -%>
<% } %>

<% if (type == 3) { %>
  <%-include('../salesmanDash/common.ejs') -%>
<% } %>

<% if (type == 4) { %>
  <%-include('../DeliverymanDash/common.ejs') -%>
<% } %>

<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Notes</title>
		<link rel="stylesheet" type="text/css" href="../assets/styles/notes.css">
		<script src="../assets/js/saveNotes.js"></script>
	</head>
	<body>
		<div class="box">
			<h1>Take your Note</h1>
			<form method='post'>
				<input type="text" name="name" placeholder="Note name" value="<%= nInfo.sub %>">
        <input type="Submit" name="PUSH" value="PUSH">
				<input style="margin-left: 80px;width: 20%;" type="text" placeholder="Search by id" name="search">
        <input type="hidden" name="NoteID" value="<%= nInfo.id %>">
				<input style="margin-left: 5px;width: 15%;" type="Submit" name="SEE" value="SEE"><br>
				<textarea placeholder="write here..." name="notes" id="notes" cols="46" rows="20"><%= nInfo.text %></textarea><br>
				<input type="submit" name="REFRESH" value="REFRESH">
        <input style="margin-left: 30px;" type="submit" name="PRINT" value="PRINT" onclick="'return saveFile()">
				<br><br>
				<input type="submit" name="UPDATE" value="UPDATE" <%if(srchStatus==false){%>disabled<%}%>>
        <input style="margin-left: 30px;" type="submit" name="DELETE" value="DELETE" <%if(srchStatus==false){%>disabled<%}%>>

      </form>
			<div align="right" class="table">
				<table class="content-table">
					<thead>
						<tr>
							<th>Note Id</th>
							<th>Note Name</th>
						</tr>
					</thead>
					<tbody class="tab">
						<% for(var i=0; i < list.length; i++){ %>
							<tr>
							  <td><%= list[i].NoteID %></td>
							  <td><%= list[i].NoteName %></td>
							</tr>
						<% } %>
					</tbody>
				
				</table>
			</div>
		</div>
	</body>
</html>
