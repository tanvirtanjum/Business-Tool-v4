<!-- sidebar Included -->

<%-include('../common.ejs') -%>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="../assets/styles/common.css">
    <link rel="stylesheet" type="text/css" href="../assets/styles/pendingOrder.css">
    <title>Pending Order List</title>
</head>
<body>
    <div class="box">
        <form method="POST">
            <!--<input style="width: 25%; margin-left: 530px;" id="search" type="text" name="Search" placeholder="Search" value="">
            <input id="search" type="submit" value="Search">
            <p>jani nah</p>
            <input type="text" placeholder="sommething">
            <p>jani nah</p>
            <input type="text" placeholder="sommething">
            <p>jani nah</p>
            <input type="text" placeholder="sommething">
            <p>jani nah</p>
            <input type="text" placeholder="sommething"><br>-->
        </form>
        <div align="center" class="table">
            <table class="content-table">
                <thead>
                    <tr>
                        <th>Order#</th>
                        <th>Date</th>
                        <th>Order Total</th>
                        <th>Pending Status</th>
                        <th> </th>
                    </tr>
                </thead>
                <tbody>
                <% if(list.length>0){ %>
                  <% for(var i=0; i < list.length; i++){ %>
                  <tr>
                    <td><%= list[i].orderid %></td>
                    <td><%= list[i].ord_date %></td>
                    <td><%= list[i].ammout %></td>
                    <td>Pending</td>
                    <td><a href="/customerDash/pendingOrders/<%=list[i].orderid%>"><input type="submit" value="CANCEL"></a><td>
                  </tr>
                  <% } %>
                <% } %>
                <% if(list.length==0){ %>
                  <tr>
                    <td colspan="5" align="center">NO DATA</td>
                  </tr>
                <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
